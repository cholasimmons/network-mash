# Main.gd
extends Node2D

#@onready var netmanager :Node = $NetworkManager

func _ready():
	pass
	#for i in netmanager.max_players:
		#var player = preload("res://scenes/Player.tscn").instantiate()
		#add_child(player)
		#player.global_position = Vector2(i + 100, i + 100)
